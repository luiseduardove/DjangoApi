const errorMessage = document.getElementById('error-message');

if (errorMessage) {
    console.log(errorMessage.textContent); // Muestra el mensaje de error en consola
    // Puedes utilizar este mensaje para mostrar una alerta o notificación
}
